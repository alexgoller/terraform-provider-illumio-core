{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "required": [
    "transmission",
    "action"
  ],
  "additionalProperties": false,
  "properties": {
    "transmission": {
      "description": "transmission type: broadcast/multicast/unicast",
      "type": "string",
      "enum": [
        "broadcast",
        "multicast",
        "unicast"
      ]
    },
    "data_source": {
      "type": "string",
      "enum": [
        "any",
        "server",
        "endpoint"
      ],
      "description": "flow summary data source"
    },
    "network": {
      "type": "string",
      "enum": [
        "any",
        "corporate",
        "external"
      ],
      "description": "flow summary network"
    },
    "target": {
      "type": "object",
      "required": [
        "proto"
      ],
      "additionalProperties": false,
      "properties": {
        "src_port": {
          "type": "integer"
        },
        "dst_port": {
          "type": "integer"
        },
        "proto": {
          "type": "integer"
        },
        "dst_ip": {
          "type": "string",
          "description": "single destination ip address or CIDR"
        },
        "src_ip": {
          "type": "string",
          "description": "single source ip address or CIDR"
        }
      }
    },
    "action": {
      "description": "drop or aggregate the target traffic",
      "type": "string",
      "enum": [
        "drop",
        "aggregate"
      ]
    }
  }
}
